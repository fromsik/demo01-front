# AGENT.md - TimeBox Planner 구현 가이드

이 문서는 팀이 동일한 방향으로 개발하기 위한 "북극성" 문서다.

## 1. 제품 원칙
1. 웹은 반드시 로그인 기반으로 동작한다.
2. 앱은 로그인 없이 로컬 모드로 즉시 시작 가능해야 한다.
3. 핵심 목표는 1~5개로 제한한다.
4. 시간 블록은 10분 단위 + 겹침 금지 검증을 반드시 통과해야 저장된다.
5. 양식(Template)과 일별 계획(Daily Plan)은 분리 저장한다.

## 2. 구현 우선순위
- Phase 1: 오늘 계획 작성 + 검증 + 저장
- Phase 2: 양식 관리
- Phase 3: 웹 인증/접근제어
- Phase 4: 기록 조회/복사
- Phase 5: 앱 로컬 모드 고도화

## 3. 프론트엔드 규칙
- 입력 즉시 상태 반영
- 저장 실패 사유를 사용자 문장으로 명확히 노출
- 모바일에서 시간 입력이 쉬운 UI(드롭다운/타임피커) 우선
- 저장 전 이탈 경고는 추후 반드시 추가

## 4. 백엔드/데이터 규칙
- 웹 데이터는 사용자 소유권(userId)으로 분리
- API는 인증 정보 검증 후 본인 데이터만 접근 허용
- 로컬 모드 데이터는 서버 업로드 금지

## 5. 금지/유의 사항
- 양식 수정 시 과거에 생성된 일별 계획이 자동 변형되면 안 됨
- MVP 범위 밖(실시간 동기화, 공유, AI 추천)은 기본 구현에서 제외

## 6. 완료 기준(DoD)
- 목표 제한/시간 검증/겹침 검증이 동작한다
- 양식 생성/복사/기본 설정/삭제가 동작한다
- 기록 조회와 오늘 복사가 동작한다
- 웹 비로그인 상태에서 작성 기능 접근이 차단된다
